# =================================================================
# 大连市皮肤病医院绩效测算 - Oracle -> SQL Server 数据同步依赖
# =================================================================
# 运行环境: Python 3.14.5 (已验证)
# 安装命令: python -m pip install -r requirements.txt
#
# 说明: 版本号采用 >= 下限约束, 以本机已实测通过的版本为基线。
#   python-dotenv  1.2.3
#   SQLAlchemy     2.0.51
#   oracledb       4.0.2
#   pandas         3.0.3
#   pyodbc         5.3.0
# =================================================================

# Oracle 官方新一代驱动 (替代 cx_Oracle, 支持 thin mode 无需安装 Oracle Client)
oracledb>=2.0.0

# SQL Server ODBC 驱动封装 (配合 ODBC Driver 17 for SQL Server)
pyodbc>=5.0.0

# 数据库工具箱 (本工程用于连接测试与元数据核查, 不在大表热路径使用)
SQLAlchemy>=2.0.0

# 数据分析 (用于同步结果核对与报表输出)
pandas>=2.0.0

# 配置文件隔离, 避免连接串硬编码入库
python-dotenv>=1.0.0
