# =================================================================
# 核心系统规范与 AI 行为锚点 (System Rules & AI Anchors)
# =================================================================
# 修改日志：
# 2026-09-12 20:30:00 | 规则扩展 | 追加第 7.1 节 编码字段物理类型标准（Code String Specification）：锁定全部编码类字段为 VARCHAR/NVARCHAR 字符串语义，严禁 CAST 为 INT/BIGINT 防前导零丢失与关联失配；新增【源列零改造优先】条款禁止对已是字符串的列叠加冗余 CAST（防隐性窄化截断与索引 SARGability 衰减）；新增【编码序列化宽度】条款约束审计文本/JSON 拼接宽度按源物理类型上限对齐（TINYINT→3/SMALLINT→6/INT→11/BIGINT→20，字符串编码按源列声明宽度）。
# 2026-09-12 17:30:00 | 规则扩展 | 追加第 9 节 SQL 模板占位符标准规范（统一占位符格式为 '{year}'、'{month}'、{struct_codes}）
# 2026-09-07 18:40:00 | 规则扩展 | 追加第 7 节 数值精度强制标准：金额/系数/权重/比例数值计算与类型转换统一 DECIMAL(18,8)
# 2026-08-17 23:50:00 | 规则扩展 | 追加第 6 节 SQL 编写标准规范（全局禁 GO 协议 / 分号结束符 / SQL 优先与程序解耦）
# 2026-08-17 21:58:00 | 规则扩展 | 追加第 4 节 EAV-Hybrid 极简持久化规范与第 5 节 Envelope Pattern 脚本封装规范
# =================================================================



## ── 1. 绝对行为红线 (Critical Constraints) ──
- 【真相优先于顺从】：严禁盲目顺应错误假设。若发现现有 SQL、清洗规则或元数据存在逻辑漏洞或增熵倾向，必须立即触发纠偏，严禁直接生成错误代码。
- 【严禁整段硬编码】：在编写或重构 SQL/提示词任务时，禁止直接复读未改动的整段代码。除非显式要求，否则必须通过结构化的"逻辑映射规则"或仅输出 Diff 增量来引导修改，杜绝生成大量冗余的硬编码块。
- 【保留模板占位符】：任何格式化、重构或逻辑升级动作，必须死守并严格保留原有脚本中特有的所有特殊模板占位符（如 `{...}` 块、`~`、参数标记等），严禁破坏模板语法。

## ── 2. 修改记录与数据血缘规范 (Metadata & Pedigree) ──
- 【强制追加日志】：任何对 SQL 脚本、存储过程或规则配置的改动，必须在文件最顶部的修改日志注释块中，按时间倒序或顺序累加一条明确的变更记录（包含：精确到秒的时间 YYYY-MM-DD HH:mm:ss | 变更核心动作分类：具体修改说明）。严禁覆盖、抹除历史日志。
- 【字段命名标准】：元数据及大数据的字段命名必须遵循全大写、全英文、下划线分割（`UPPER_CASE_SNAKE`），严禁使用拼音或模糊指代，确保标准中台分层逻辑血缘清晰。

### ── Skill: 绩效二次分配公式/计算过程/详解描述硬性表达规范 ──
在所有 SQL 绩效核算脚本中，凡涉及 `[计算过程描述]` 或类似文本拼接字段，必须严格遵循“语义与数学解耦”的三段式/四段式标准结构：

【结构标准】
[元数据段] | [中文逻辑公式段] | [纯数学代入算式段] | [纯数字结算算式段]

【绝对零汉字法则（数学段落）】
1. **中文逻辑公式段（第二段）**：负责承载所有业务语义（如 `普通员工基础绩效 = 统一剩余池 × 80% × (岗位系数 × 出勤率 × 考核系数) ÷ 全科普通员工总权重`）。
2. **纯数学代入算式段（第三段）**：**严禁出现任何汉字或括号说明**（如严禁出现 `剩余池(6156.34)` 或 `岗位系数(1.00)`）。必须且仅能包含数字、运算符（` × `、` ÷ `、` + `、` - `、` = `）、百分号（`%`）及括号（`()`、`[]`）。
   - 正确示例：`6156.34 × 80% × [1.0000 × (31 ÷ 31 = 1.0000) × 1.0000 = 1.0000] ÷ 3.0000 = 1641.69`
3. **纯数字结算算式段（第四段）**：**严禁出现任何汉字**。
   - 正确示例：`1641.69 + 0.00 = 1641.69`
4. **排版格式**：
   - 所有分割管道符 ` | ` 前后保留 1 个空格。
   - 所有数学运算符（` × `、` ÷ `、` + `、` - `、` = `）两侧必须强制保留 1 个半角空格。

## ── 3. 交互纠偏协议 (Interaction Protocol) ──
- 【结论先行】：剔除所有社交寒暄与模糊措辞。输出格式固定为：方案/审计结论 + 核心支撑点 + 立即执行的动作。
- 【隔离多余维度】：在处理报表、分组和聚合优化时，必须死守最原始的物理分组规则，若需增加辅助或计算过程字段，必须通过新设标准 CTE 进行多级链路解耦隔离，防止最外层聚合被新维度污染。

# =================================================================
# EAV-Hybrid 极简持久化与 Envelope 封装规范 (EAV-Hybrid & Envelope Rules)
# =================================================================

## ── 4. 统一持久化架构规范 (EAV-Hybrid Architecture) ──
- 【物理列极简化】：统一持久化日志表 `DWD_FIN_CALC_DETAIL_LOG` 物理列仅允许包含粒度主键（`CALC_YEAR`, `CALC_MONTH`, `SCRIPT_NAME`, `UNIT_CODE`, `STAFF_CODE`, `POST_CODE`）以及 `FINAL_VALUE`（最终值）和 `CALC_PROCESS_TEXT`（审计文本）。
- 【过程仓收敛法则】：所有非粒度主键的描述性/过程性指标（包括但不限于：岗位系数、考勤天数、出勤率、考核系数、奖惩因子、资金池切分、各种权重与中间积分链）必须统一通过 `FOR JSON PATH` 序列化收敛至 `CALC_DETAIL_JSON`（NVARCHAR(MAX)）中。
- 【严禁 ALTER TABLE】：严禁因单一脚本或新增业务场景的差异频繁对持久化主表执行 `ALTER TABLE` 扩充物理列。

## ── 5. Envelope Pattern 脚本封装规范 ──
- 【绝对代码封箱】：原始绩效 SQL 脚本中的 `DETAIL` CTE 逻辑、模板契约输出格式（`struct_code / struct_name / result_value`）、`~` 隔离标记以及 `{year}`/`{month}`/`{struct_codes}` 占位符严禁作任何侵入式修改。
- 【幂等重跑机制】：所有持久化封装必须遵循“DELETE 先行，INSERT 在后”的幂等模式。执行 INSERT 前必须先依据 `(CALC_YEAR, CALC_MONTH, SCRIPT_NAME, UNIT_CODE)` 清理历史重算数据，杜绝脏数据。
- 【字段映射标准】：封装层转换时，必须严格执行字段语义对齐（如 `[核算年份] AS CALC_YEAR`, `[员工分配金额]/[积分] AS FINAL_VALUE`），并确保所有 JSON 节点字段符合全大写下划线命名规范（`UPPER_CASE_SNAKE`）。

# =================================================================
# SQL 编写标准规范 (SQL Writing Standards)
# =================================================================

## ── 6. SQL 编写标准规范 ──
- 【全局禁 GO 协议】：严格禁止在任何 SQL 脚本中使用 `GO` 批处理指令。所有 SQL 必须支持单批处理执行，保证在 Java/C# 后端驱动、DataX 传输或各类 DB 客户端下零修改通用。
- 【语句结束符】：多条 DML/DDL 连续执行时，每条独立语句末尾必须显式使用英文分号 `;`。
- 【SQL 优先与程序解耦】：所有字段转换、别名映射与格式清洗必须在 SQL 内部闭环，禁止因 SQL 格式问题推动程序层代码修改。

# =================================================================
# 数值精度强制标准 (Data Precision Specification)
# =================================================================

## ── 7. 全局精度强制标准 ──
- 【统一 DECIMAL(18,8)】：除显式特殊声明外，所有 SQL 脚本中的金额、系数、权重、比例、人数与各类绩效中间数值的计算过程与类型转换，必须统一使用 `DECIMAL(18,8)`；落库 `FINAL_VALUE` 采用 DDL 既定 `DECIMAL(18,8)`，杜绝因低精度截断累积误差。
- 【严禁低精度类型】：严禁在数值计算与类型落库中引入 `DECIMAL(18,4)`/`DECIMAL(18,2)` 或未定精度的 `FLOAT`/`REAL`（展示型 FLOAT 需在导出/展示层再收敛）；审计文本数值拼接 CAST 亦同步采用 `DECIMAL(18,8)`，保证文本推演与实际计算全程同精度同源。
- 【兜底零值同精度】：空值兜底常量（如 0 / -1）在显式 CAST 时沿用 `DECIMAL(18,8)` 字面形态，保持审计文本的 0.00000000 与全部数值列精度一致。

## ── 7.1 编码字段物理类型标准 (Code String Specification) ──
- 【绝对字符串法则】：系统中所有的编码类字段（包括但不限于：`UNIT_CODE` 核算单元编码、`DEPT_CODE` 科室编码、`STAFF_CODE` 员工工号、`PROJ_CODE` 项目编码、`ITEM_CODE` 核算项编码、`POST_CODE` 岗位编码等），物理存储与逻辑处理必须统一强制为字符串类型（`VARCHAR` / `NVARCHAR`）。
- 【严禁转为数值】：哪怕编码的字面量形态全由无意义纯数字组成（如 `'0030'`、`'1001'`），也严禁在 SQL 显式或隐式执行 `CAST(CODE AS INT)` / `CAST(CODE AS BIGINT)`。转换会导致前导零丢失（如 `'0030'` 变 `30`）、字符串匹配截断及关联全量失配。
- 【关联归一化】：不同数据源间编码格式不一致时，必须通过字符串填充函数（如 `RIGHT('0000' + LTRIM(RTRIM(CODE)), N)`）做规范化拼接，禁止通过转换为整型来规避格式问题。
- 【源列零改造优先】：源列本身已是字符串类型时，关联与输出必须**裸引用原列**，严禁叠加无业务意义的 `CAST(COL AS VARCHAR(N))` 冗余转换。冗余 CAST 不仅会引入隐性窄化截断风险（源列位数大于 N 时静默截断、摧毁长编码血缘），还会阻断索引 SARGability 并使血缘无法分辨原生字符串与改造后字符串。仅当源列确为非字符串类型时，方可做**一次性、字段实际最大长度对齐**的字符串化转换。
- 【编码序列化宽度】：编码类字段（含代理主键标识符，如自增 `ID`）在审计文本或 JSON 拼接中序列化为字符串时，宽度必须按**源物理类型实际上限对齐**，严禁使用小于源类型表达能力的宽度。对齐基准：`TINYINT`→`VARCHAR(3)`、`SMALLINT`→`VARCHAR(6)`、`INT`→`VARCHAR(11)`、`BIGINT`→`VARCHAR(20)`；业务字符串编码按其源列声明宽度对齐（如 `varchar(50)`→`VARCHAR(50)`、`varchar(300)`→`VARCHAR(300)`）。



# =================================================================
# dbt 工程与建模规范 (dbt Modeling & Engineering Standards)
# =================================================================

## ── 8. dbt 建模与代码表达标准 ──

### 8.1 引用协议与三段式 CTE 链条
- 【绝对引用协议】：严禁在 dbt 模型 SQL 中硬编码数据库或 Schema 全路径（如 `DB.dbo.TABLE`）。Staging 层数据源必须统一使用 `{{ source('source_name', 'table_name') }}`；Intermediate 与 Marts 层必须统一使用 `{{ ref('model_name') }}` 维护血缘。
- 【标准 CTE 结构】：所有 dbt 模型必须遵循 dbt-labs 官方三段式 CTE 链条，严禁逻辑混写：
  1. **Import CTEs**：顶部仅做 `ref`/`source` 显式引入与字段挑选（如 `stg_orders as (select * from {{ ref('...') }})`）。
  2. **Logical CTEs**：中间完成业务逻辑解耦、计算与多表 Join（如 `detail` / `calc`）。
  3. **Final CTE**：末尾统一收敛至 `final` CTE，最下方固定写死 `SELECT * FROM final;`，严禁在终表进行复杂计算。

### 8.2 数据质量断言与 Schema 契约
- 【强制主键断言】：每一个在 `models/marts` 或 `models/staging` 下新建的模型，必须同步在同级 `schema.yml` 中声明字段文档，且主键列必须绑定 `unique` 与 `not_null` 两个基本数据质量测试。
- 【外键与枚举校验】：涉及科室、员工、绩效项等关联外键，需在 `schema.yml` 配置 `relationships` 测试，确保源头不脱节。

### 8.3 全局目录物化与配置收敛
- 【零局部配置】：禁止在单个 `.sql` 头部大量硬编码 `{{ config(...) }}`。物化策略必须统一在 `dbt_project.yml` 中按目录继承：
  - `models/staging` 目录：统一继承 `materialized: view` 或 `ephemeral`。
  - `models/intermediate` 目录：统一继承 `materialized: ephemeral`。
  - `models/marts` 目录：统一继承 `materialized: table` 或 `incremental`。

### 8.4 粒度控制与代理键（Surrogate Key）
- 【唯一代理键】：涉及多维度联合主键的中间表与集市表（如 `核算年月 + 科室代码 + 员工工号`），必须使用 `dbt_utils.generate_surrogate_key([...])` 生成哈希物理主键（命名为 `KEY_<MODEL_NAME>`），确保数据粒度唯一且具备强幂等性。

### 8.5 增量更新与重算幂等
- 【增量模型约束】：凡声明为 `incremental` 的 Marts 层模型，必须显式定义 `unique_key`（使用 8.4 生成的代理键），并配置 `incremental_strategy = 'delete+insert'`（或对应数据库的 `merge`），保证重新跑历史月份绩效数据时严格执行覆盖覆盖重算，杜绝数据重复。

### 8.6 血缘与原生规范融合
- dbt 内部编写的所有 SQL 表达式必须继续严格死守 `.clinerules` 前序条目：
  - 字段别名全大写下划线（`UPPER_CASE_SNAKE`）。
  - 数值计算与转换强制转换为 `DECIMAL(18,8)`。
  - 过程描述文本遵循四段式/三段式硬性表达规范。
  - 单条语句末尾显式使用英文分号 `;`，全局禁用 `GO` 指令。

# =================================================================
# 模板占位符统一规范 (Template Parameter Standard)
# =================================================================

## ── 9. SQL 模板占位符强制标准 ──
- 【统一占位符语法】：所有 SQL 脚本中的动态注入参数，必须且仅能使用以下标准占位符，严禁引入任何自定义或废弃参数（如 `{cal_year}`、`{cal_month}`、`{version_no}` 等）：
  - `'{year}'` ：核算年份（带单引号文本，如 `'2025'`）
  - `'{month}'`：核算月份（带单引号文本，如 `'6'`）
  - `{struct_codes}`：科室代码过滤集合（无单引号，如 `'1001','1002'` 或 1=1 降维表达式）
- 【参数格式硬性约束】：在 SQL 内部与 `DATEFROMPARTS` 等函数组合使用时，必须通过 `CAST('{year}' AS INT)` / `CAST('{month}' AS INT)` 进行强类型转换，确保 T-SQL 语法一致性。
- 【版本快照寻址解耦】：维度版本（`VERSION_NO`）寻址由外部方案调度协议负责，严禁以模板占位符形式下沉至 SQL 脚本内部；脚本内需锁定最新版本快照时，必须通过 `ROW_NUMBER()` 排序后收敛 `VERSION_RANK = 1` 实现，杜绝版本分支条件污染计算链路。

